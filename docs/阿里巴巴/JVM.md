# 阿里巴巴 - JVM

> 本文档整理自 阿里巴巴 面经真题，按知识点归类

## 目录

- [一、JVM 高频题](#一、JVM-高频题)

---

## JVM 高频题


### Q1: 说一下 JVM 内存结构？

**核心概念**：

JVM 运行时数据区分为线程私有和线程共享两大部分：

```
┌─────────────────────────────────────────────────────────────────┐
│                        JVM 运行时数据区                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────── 线程私有 ───────────────────┐            │
│  │                                               │            │
│  │  ┌────────────┐ ┌────────────┐ ┌───────────┐ │            │
│  │  │程序计数器   │ │ 虚拟机栈   │ │本地方法栈 │ │            │
│  │  │PC Register │ │ VM Stack  │ │Native Stack│ │            │
│  │  └────────────┘ └────────────┘ └───────────┘ │            │
│  │                                               │            │
│  └───────────────────────────────────────────────┘            │
│                                                                 │
│  ┌─────────────────── 线程共享 ───────────────────┐            │
│  │                                               │            │
│  │  ┌─────────────────────────────────────────┐  │            │
│  │  │              堆 (Heap)                  │  │            │
│  │  │  ┌────────────┬────────────────────┐   │  │            │
│  │  │  │ 新生代      │      老年代        │   │  │            │
│  │  │  │ Young Gen  │    Old Gen        │   │  │            │
│  │  │  └────────────┴────────────────────┘   │  │            │
│  │  └─────────────────────────────────────────┘  │            │
│  │                                               │            │
│  │  ┌─────────────────────────────────────────┐  │            │
│  │  │            方法区 (Method Area)         │  │            │
│  │  │  类信息、常量、静态变量、JIT代码         │  │            │
│  │  └─────────────────────────────────────────┘  │            │
│  │                                               │            │
│  └───────────────────────────────────────────────┘            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**详细解答**：

**1. 程序计数器（Program Counter Register）**
- **作用**：记录当前线程执行到的字节码行号
- **特点**：唯一不会 OOM 的区域，线程私有
- **为什么需要**：CPU 多线程切换后，需要知道恢复到哪一行继续执行

**2. 虚拟机栈（VM Stack）**
- **作用**：每个方法执行时创建一个栈帧，存储局部变量表、操作数栈、动态链接、返回地址
- **异常**：StackOverflowError（递归过深）、OutOfMemoryError（栈空间不足）
- **配置**：`-Xss256k` 设置单个线程栈大小

**栈帧结构**：
```
┌─────────────────────────────────────────┐
│              栈帧 (Stack Frame)          │
├─────────────────────────────────────────┤
│  ┌─────────────────────────────────┐   │
│  │        局部变量表                │   │
│  │  [0] this                       │   │
│  │  [1] 参数1                       │   │
│  │  [2] 局部变量1                   │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │        操作数栈                  │   │
│  │   用于计算的中间结果存储          │   │
│  └─────────────────────────────────┘   │
│                                         │
│  动态链接、方法返回地址                   │
└─────────────────────────────────────────┘
```

**3. 本地方法栈**
- 为 Native 方法服务，与虚拟机栈类似

**4. 堆（Heap）**
- **作用**：存储对象实例，GC 的主要区域
- **结构**：新生代（Eden + S0 + S1）+ 老年代
- **配置**：`-Xms2g -Xmx2g` 设置初始和最大堆大小

**堆内存划分**：
```
┌────────────────────────────────────────────────────────────────┐
│                           堆 (Heap)                             │
├────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────新生代 (1/3)────────────────────┐  │
│  │  ┌──────────────────────────────────────────────────┐   │  │
│  │  │              Eden 区 (8)                          │   │  │
│  │  └──────────────────────────────────────────────────┘   │  │
│  │  ┌────────────────────┐  ┌────────────────────┐        │  │
│  │  │  Survivor S0 (1)   │  │  Survivor S1 (1)   │        │  │
│  │  └────────────────────┘  └────────────────────┘        │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────老年代 (2/3)───────────────────┐  │
│  │                                                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
默认比例：Eden:S0:S1 = 8:1:1，新生代:老年代 = 1:2
```

**5. 方法区（Method Area）**
- **JDK 1.6 及之前**：永久代（PermGen），在 JVM 堆中
- **JDK 1.8 及之后**：元空间（Metaspace），在本地内存中
- **存储内容**：类信息、常量、静态变量、JIT 编译后的代码

**面试加分点**：
> 永久代改为元空间的原因：
> 1. 永久代大小难确定，容易 OOM
> 2. 类卸载效率低，GC 复杂
> 3. 统一使用本地内存，与 HotSpot 内存管理模型一致

**追问延伸**：
- 堆和栈的区别是什么？
  - 堆存对象实例，线程共享；栈存栈帧和局部变量，线程私有
  - 堆空间大（GB级），栈空间小（MB级）
  - 堆是 GC 主要区域，栈不需要 GC

- 对象一定在堆上分配吗？
  - 不一定！如果对象不逃逸（Escape Analysis），可能栈上分配
  - 逃逸分析：判断对象是否会逃逸出方法
  
```java
// 对象逃逸，不能栈上分配
public User getUser() {
    User user = new User();
    return user;  // 逃逸出方法
}

// 对象不逃逸，可能栈上分配
public void process() {
    User user = new User();  // 可能分配在栈上
    user.setId(1);
}  // 方法结束，对象自动销毁
```

- 方法区在不同 JDK 版本的变化？
  - JDK 1.6：永久代在 JVM 堆
  - JDK 1.7：字符串常量池移到堆
  - JDK 1.8：永久代完全移除，改为元空间（本地内存）

---

### Q2: 垃圾收集算法有哪些？

**核心概念**：

垃圾收集需要解决两个问题：**哪些内存需要回收**、**什么时候回收**、**如何回收**

**1. 判断对象存活**

**引用计数法**：
```java
// 问题：循环引用无法回收
public class ReferenceCountingGC {
    public Object instance = null;
    
    public static void main(String[] args) {
        ReferenceCountingGC objA = new ReferenceCountingGC();
        ReferenceCountingGC objB = new ReferenceCountingGC();
        objA.instance = objB;
        objB.instance = objA;
        objA = null;
        objB = null;
        // 循环引用，引用计数不为 0，但实际已无引用
    }
}
```

**可达性分析（JVM 使用）**：
- 从 GC Roots 开始，遍历对象图
- 不可达的对象即为垃圾

**GC Roots 包括**：
1. 虚拟机栈中引用的对象
2. 方法区中类静态属性引用的对象
3. 方法区中常量引用的对象
4. 本地方法栈中 JNI 引用的对象
5. 同步锁持有的对象

**2. 垃圾收集算法**

**标记-清除（Mark-Sweep）**：
```
标记前：[对象A][垃圾][对象B][垃圾][垃圾][对象C]
标记后：[对象A][  垃圾 ][对象B][  垃圾  ][垃圾][对象C]
清除后：[对象A][      ][对象B][         ][   ][对象C]
                          ↑         ↑
                        碎片       碎片
```
- **优点**：简单
- **缺点**：效率低，有内存碎片

**标记-整理（Mark-Compact）**：
```
标记后：[对象A][  垃圾 ][对象B][  垃圾  ][垃圾][对象C]
整理后：[对象A][对象B][对象C][                   ]
                                           ↑
                                       连续空闲空间
```
- **优点**：无碎片
- **缺点**：移动对象有开销

**复制算法（Copying）**：
```
复制前：
┌─────────────────From─────────────────┐
│[对象A][垃圾][对象B][垃圾][垃圾][对象C]│
└──────────────────────────────────────┘

复制后：
┌─────────────────To──────────────────┐
│[对象A][对象B][对象C][               ]│
└──────────────────────────────────────┘
```
- **优点**：高效，无碎片
- **缺点**：空间利用率低（需要 50% 空间作为保留区）

**分代收集**：
- 新生代：存活率低，用复制算法
- 老年代：存活率高，用标记-整理或标记-清除

**面试加分点**：
> 为什么新生代用复制算法？
> - 新生代对象存活率低（约 90% 都是垃圾）
> - 复制算法只需复制少量存活对象，效率高
> - 实际使用 Eden:S0:S1 = 8:1:1 的比例，只用 10% 作为保留区

**追问延伸**：
- CMS 用什么算法？有什么问题？
  - 用标记-清除算法，有内存碎片
  - 解决方案：开启 `-XX:+UseCMSCompactAtFullCollection` 压缩

- G1 的 Region 设计？
  - 将堆划分为多个大小相等的 Region（1-32MB）
  - 每个 Region 可以是 Eden、Survivor 或 Old
  - 优先收集垃圾最多的 Region（Garbage First）

---

### Q3: 说一下 CMS 收集器？

**核心概念**：

CMS（Concurrent Mark Sweep）是第一款真正意义上的并发收集器，让垃圾收集线程与用户线程同时运行。

**四个阶段**：

```
┌─────────────────────────────────────────────────────────────────┐
│                        CMS 收集过程                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 初始标记（STW）                                              │
│     └─ 标记 GC Roots 直接关联的对象                              │
│     └─ 速度很快                                                 │
│                                                                 │
│  2. 并发标记                                                     │
│     └─ 遍历对象图，与用户线程并发                                │
│     └─ 时间长，但不阻塞用户线程                                  │
│                                                                 │
│  3. 重新标记（STW）                                              │
│     └─ 修正并发标记期间的变动（增量更新）                         │
│     └─ 修复"浮动垃圾"问题                                        │
│                                                                 │
│  4. 并发清除                                                     │
│     └─ 清除标记为垃圾的对象                                      │
│     └─ 与用户线程并发                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**代码示例**：

```bash
# 启用 CMS 收集器（JDK 1.8 默认使用 Parallel，需手动开启）
-XX:+UseConcMarkSweepGC
-XX:CMSInitiatingOccupancyFraction=75  # 老年代 75% 时触发
-XX:+UseCMSCompactAtFullCollection     # Full GC 时压缩
-XX:CMSFullGCsBeforeCompaction=0       # 每次 Full GC 都压缩
```

**优缺点分析**：

| 优点 | 缺点 |
|------|------|
| 并发收集，低停顿 | CPU 敏感（占用线程资源） |
| 老年代收集首选 | 浮动垃圾（并发期间新产生的垃圾） |
| 成熟稳定 | 内存碎片（标记-清除算法） |

**面试加分点**：
> CMS 的"三色标记"算法：
> - 白色：未被访问
> - 灰色：已被访问，但引用的对象未全部访问
> - 黑色：已被访问，且引用的对象全部访问
> 
> 问题：并发标记期间，用户线程可能修改引用，导致"对象消失"
> 解决：增量更新（Incremental Update）或原始快照（SATB）

**追问延伸**：
- CMS 和 G1 的区别？

| 维度 | CMS | G1 |
|------|-----|-----|
| 算法 | 标记-清除 | 标记-整理 + 复制 |
| 内存布局 | 连续新生代/老年代 | Region 分区 |
| 碎片 | 有 | 无 |
| 可预测停顿 | 不支持 | 支持（`MaxGCPauseMillis`） |
| JDK 版本 | JDK 5 引入，JDK 9 标记废弃 | JDK 7 引入，JDK 9 默认 |

- 为什么 CMS 要用标记-清除不用标记-整理？
  - 并发清除时，如果移动对象，需要更新引用，与用户线程冲突
  - G1 通过 Region 隔离解决了这个问题

---

### Q4: 类加载过程？

**核心概念**：

类加载分为 5 个阶段：加载 → 验证 → 准备 → 解析 → 初始化

**详细过程**：

```
┌─────────────────────────────────────────────────────────────────┐
│                        类加载过程                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 加载（Loading）                                              │
│     └─ 读取 .class 文件到内存                                    │
│     └─ 创建 Class 对象                                           │
│     └─ 可以从文件系统、网络、ZIP 包加载                          │
│                                                                 │
│  2. 验证（Verification）                                         │
│     └─ 文件格式验证（魔数 0xCAFEBABE）                            │
│     └─ 字节码验证                                                │
│     └─ 符号引用验证                                              │
│                                                                 │
│  3. 准备（Preparation）                                          │
│     └─ 为静态变量分配内存，设初始值                              │
│     └─ 注意：final 变量直接赋真实值                              │
│                                                                 │
│  4. 解析（Resolution）                                           │
│     └─ 符号引用 → 直接引用                                       │
│     └─ 类/接口、字段、方法解析                                   │
│                                                                 │
│  5. 初始化（Initialization）                                     │
│     └─ 执行 <clinit> 方法（静态变量赋值 + 静态代码块）            │
│     └─ 父类先于子类初始化                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**代码示例**：

```java
public class ClassInitDemo {
    // 准备阶段：value = 0
    // 初始化阶段：value = 123
    private static int value = 123;
    
    // 准备阶段：CONSTANT = 456（final 直接赋值）
    private static final int CONSTANT = 456;
    
    static {
        System.out.println("静态代码块执行");
    }
    
    public static void main(String[] args) {
        System.out.println(ClassInitDemo.value);  // 触发类初始化
    }
}
```

**双亲委派模型**：

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                    ┌────────────────────┐                       │
│                    │  Bootstrap Class   │                       │
│                    │  Loader            │                       │
│                    │  (核心类库)         │                       │
│                    └─────────┬──────────┘                       │
│                              │                                  │
│                    ┌─────────▼──────────┐                       │
│                    │  Extension Class   │                       │
│                    │  Loader            │                       │
│                    │  (扩展类库)         │                       │
│                    └─────────┬──────────┘                       │
│                              │                                  │
│                    ┌─────────▼──────────┐                       │
│                    │  Application Class │                       │
│                    │  Loader            │                       │
│                    │  (应用类路径)       │                       │
│                    └─────────┬──────────┘                       │
│                              │                                  │
│                    ┌─────────▼──────────┐                       │
│                    │  Custom Class      │                       │
│                    │  Loader            │                       │
│                    └────────────────────┘                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**双亲委派流程**：
1. 收到类加载请求
2. 先委托父类加载器加载
3. 父类无法加载，才自己加载

**为什么需要双亲委派？**
1. **安全**：防止核心类被篡改（如自定义 `java.lang.String`）
2. **避免重复加载**：父类已加载，子类不再加载

**如何打破双亲委派？**
1. **自定义类加载器**：重写 `loadClass()` 方法
2. **SPI 机制**：JDBC Driver 使用线程上下文类加载器

```java
// 打破双亲委派示例
public class CustomClassLoader extends ClassLoader {
    @Override
    public Class<?> loadClass(String name) throws ClassNotFoundException {
        // 先尝试自己加载
        try {
            byte[] data = loadClassData(name);
            return defineClass(name, data, 0, data.length);
        } catch (Exception e) {
            // 失败则委托父类
            return super.loadClass(name);
        }
    }
}
```

**追问延伸**：
- 什么时候触发类初始化？
  1. 遇到 `new`、`getstatic`、`putstatic`、`invokestatic`
  2. 反射调用 `Class.forName()`
  3. 初始化子类时，父类未初始化
  4. 主类（main 方法所在类）

---


