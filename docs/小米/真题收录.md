# 小米 - 真题收录

> 本文档整理自小米社招面经真题，按知识点归类

---

## 目录

- [小米一面真题](#小米一面真题)
- [小米二面真题](#小米二面真题)

---

## 小米一面真题

**来源**：[牛客网](https://www.nowcoder.com/discuss/742337370053238784)
**时间**：2025-04-17
**背景**：社招 Java 开发

### Q1: JVM 由哪些部分组成？

**参考答案**：

**JVM 主要组成部分**：
1. **类加载器子系统**（ClassLoader）
2. **运行时数据区**（Runtime Data Area）
3. **执行引擎**（Execution Engine）
4. **本地方法接口**（JNI）

---

### Q2: JVM 的内存结构？

**参考答案**：

**运行时数据区**：
| 区域 | 说明 |
|------|------|
| 方法区 | 类信息、常量、静态变量 |
| 堆 | 对象实例 |
| 虚拟机栈 | 方法调用栈帧 |
| 本地方法栈 | Native 方法调用 |
| 程序计数器 | 当前执行行号 |

---

### Q3: 你知道有哪些垃圾回收器吗？

**参考答案**：

**垃圾收集器列表**：
| 收集器 | 说明 |
|--------|------|
| Serial | 单线程，适合客户端 |
| ParNew | Serial 多线程版 |
| Parallel Scavenge | 关注吞吐量 |
| CMS | 并发标记清除，低停顿 |
| G1 | Region 分区，可预测停顿 |
| ZGC | 低延迟，大堆内存 |

---

### Q4: 能细聊一下 G1 吗？

**参考答案**：

**G1 工作流程**：
- **并发标记阶段**：标记存活对象
- **对象拷贝阶段**：将存活对象拷贝到新 Region

**G1 特点**：
1. Region 分区设计
2. 可预测停顿时间（`MaxGCPauseMillis`）
3. 无内存碎片
4. 适合大堆内存（> 6G）

---

### Q5: MySQL 的范式了解吗？

**参考答案**：

**三大范式**：
| 范式 | 说明 |
|------|------|
| 第一范式 | 字段不可分割 |
| 第二范式 | 非主键字段完全依赖主键 |
| 第三范式 | 非主键字段不传递依赖主键 |

---

### Q6: MySQL 的索引有哪些？

**参考答案**：

**索引类型**：
| 类型 | 说明 |
|------|------|
| 主键索引 | 唯一标识记录 |
| 唯一索引 | 字段值唯一 |
| 普通索引 | 加速查询 |
| 全文索引 | 文本搜索 |
| 组合索引 | 多列组合 |

**注意事项**：
1. 遵循最左前缀原则
2. 索引不是越多越好
3. 避免`SELECT *`

---

### Q7: 树的高度跟磁盘 IO 操作有什么关联？

**参考答案**：

**B+ 树优势**：
- 树的高度直接影响 IO 次数
- B+ 树高度低（通常 3 层），IO 次数少
- 叶子节点存储数据，减少磁盘访问

---

### Q8: HTTPS 握手过程？

**参考答案**：

**HTTPS 握手**：
1. 客户端发送支持的加密算法列表
2. 服务端选择算法，返回证书
3. 客户端验证证书，生成随机数
4. 用公钥加密随机数发送给服务端
5. 服务端私钥解密，生成会话密钥
6. 后续用会话密钥加密通信

---

### Q9: 非对称加密时客户端是用私钥加密还是公钥？

**参考答案**：

**加密规则**：
- 公钥加密，私钥解密（保密性）
- 私钥签名，公钥验证（身份认证）

---

### Q10: 算法题 - 判断回文串

**参考答案**：

```java
public boolean isPalindrome(String s) {
    int left = 0, right = s.length() - 1;
    while (left < right) {
        while (left < right && !Character.isLetterOrDigit(s.charAt(left))) left++;
        while (left < right && !Character.isLetterOrDigit(s.charAt(right))) right--;
        if (Character.toLowerCase(s.charAt(left)) != Character.toLowerCase(s.charAt(right))) {
            return false;
        }
        left++;
        right--;
    }
    return true;
}
```

---

### Q11: 算法题 - 反转链表

**参考答案**：

```java
public ListNode reverseList(ListNode head) {
    ListNode prev = null, curr = head;
    while (curr != null) {
        ListNode next = curr.next;
        curr.next = prev;
        prev = curr;
        curr = next;
    }
    return prev;
}
```

---

### Q12: 如何平滑迁移数据库？

**参考答案**：

**不停服迁移方案**：
1. 双写阶段：同时写新旧数据库
2. 数据同步：迁移历史数据
3. 校验数据：对比新旧数据一致性
4. 切换读流量：逐步切换到新库
5. 停止写旧库：完成迁移

---

### Q13: Redis 用在哪些场景？

**参考答案**：

**常见场景**：
- 缓存热点数据
- 分布式锁
- 排行榜（ZSet）
- 计数器
- Session 共享
- 消息队列

---

### Q14: 缓存击穿、缓存穿透和缓存雪崩？

**参考答案**：

| 问题 | 说明 | 解决方案 |
|------|------|---------|
| 缓存穿透 | 查询不存在数据 | 布隆过滤器、空值缓存 |
| 缓存击穿 | 热点 Key 过期 | 永不过期、互斥锁 |
| 缓存雪崩 | 大量 Key 同时过期 | 随机过期时间、多级缓存 |

---

### Q15: 读写锁的原理是什么？

**参考答案**：

**ReentrantReadWriteLock**：
- 读锁共享，多个线程可同时读
- 写锁独占，写时阻塞读和其他写
- 锁降级：写锁 → 读锁
- 不支持锁升级：读锁 → 写锁

---

### Q16: Redis 的底层数据结构有哪些？

**参考答案**：

| 类型 | 底层实现 |
|------|---------|
| String | SDS |
| List | QuickList |
| Hash | HashTable |
| Set | HashTable + IntSet |
| ZSet | SkipList + HashTable |

---

### Q17: HashMap 的工作原理是什么？

**参考答案**：

**HashMap 底层**：
- JDK 8：数组 + 链表 + 纛黑树
- 负载因子：0.75
- 扩容：容量翻倍
- 链表转红黑树：长度 ≥ 8

---

### Q18: ConcurrentHashMap 的原理是什么？

**参考答案**：

**JDK 8 实现**：
- CAS + synchronized
- 锁单个 Node，并发度高
- 取消分段锁设计

---

### Q19: 一条 URL 在网页输入的过程中会发生什么？

**参考答案**：

**流程**：
1. DNS 解析
2. TCP 连接
3. HTTP 请求
4. 服务端处理
5. HTTP 响应
6. 浏览器渲染

---

### Q20: 进程和线程有什么区别？

**参考答案**：

| 对比项 | 进程 | 线程 |
|--------|------|------|
| 资源 | 独立地址空间 | 共享进程资源 |
| 通信 | IPC | 共享变量 |
| 开销 | 大 | 小 |
| 崩溃影响 | 独立 | 影响整个进程 |

---

### Q21: MySQL 的底层索引结构是什么？

**参考答案**：

**B+ 树特点**：
- 叶子节点存储数据
- 非叶子节点只存索引
- 叶子节点双向链表
- 高度低（通常 3 层）

---

### Q22-24: 项目相关问题

**参考答案**：

**实习相关问题**：
- 负责的具体内容
- 遇到的问题及解决过程
- 项目难点和收获

---

### Q25: 算法题 - 找第 K 大元素

**参考答案**：

```java
public int findKthLargest(int[] nums, int k) {
    PriorityQueue<Integer> pq = new PriorityQueue<>();
    for (int num : nums) {
        pq.offer(num);
        if (pq.size() > k) {
            pq.poll();
        }
    }
    return pq.peek();
}
```

---

### Q26: 算法题 - 检测环形链表

**参考答案**：

```java
public boolean hasCycle(ListNode head) {
    ListNode slow = head, fast = head;
    while (fast != null && fast.next != null) {
        slow = slow.next;
        fast = fast.next.next;
        if (slow == fast) return true;
    }
    return false;
}
```

---

## 小米二面真题

### Q27: 主线程等待子线程执行完毕有哪些方法？

**参考答案**：

**方案**：
1. `CountDownLatch`
2. `CyclicBarrier`
3. `join()`
4. `Future.get()`

---

### Q28: 线程池拒绝策略有哪些？

**参考答案**：

| 策略 | 说明 |
|------|------|
| AbortPolicy | 抛异常 |
| CallerRunsPolicy | 调用者执行 |
| DiscardPolicy | 静默丢弃 |
| DiscardOldestPolicy | 丢弃最老任务 |

---

### Q29: 索引失效的情况有哪些？

**参考答案**：

**失效场景**：
1. 使用函数：`WHERE YEAR(date) = 2026`
2. 类型转换：`WHERE id = '123'`（id 是 INT）
3. `LIKE '%xxx'`
4. `OR` 条件部分无索引
5. 索引列参与计算

---

### Q30: 意向锁有什么作用？

**参考答案**：

**意向锁**：
- 表级锁，表示事务意图
- 提升锁兼容性检查效率
- 避免逐行检查行锁

---

### Q31: 查询语句使用 ORDER BY 要对排序字段建立索引吗？

**参考答案**：

**建议**：
- 排序字段建立索引可避免 `filesort`
- 组合索引遵循最左前缀
- 避免 `SELECT *`

---

### Q32: Spring Boot 核心特性有哪些？

**参考答案**：

**核心特性**：
1. 自动装配
2. 起步依赖
3. 内嵌服务器
4. 配置管理
5. Actuator 监控

---

### Q33-36: JVM 命令与内存分析

**参考答案**：

**常用 JVM 命令**：
- `jstat`：查看 GC 统计
- `jmap`：查看内存、dump
- `jstack`：查看线程栈
- `jinfo`：查看 JVM 参数

**内存分析**：
- MAT 分析 dump 文件
- VisualVM 实时监控
- GC 日志分析

---

**面试总结**：
- 小米面试重视基础和算法
- JVM、MySQL、Redis 是高频考点
- 算法题难度中等
- 需要准备项目经验和实习内容

---

[返回目录](README.md)