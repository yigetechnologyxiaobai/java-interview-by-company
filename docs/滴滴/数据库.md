# 滴滴 - 数据库

> 本文档整理自 滴滴 面经真题，按知识点归类

## 目录

- [二、MySQL 高频题](#二、MySQL-高频题)
- [三、Redis 高频题](#三、Redis-高频题)

---

## MySQL 高频题


### Q5: 索引为什么用 B+ 树？

**参考答案**：

**B+ 树特点**：
1. **树高度低**：3-4 层可存千万级数据
2. **范围查询友好**：叶子节点链表
3. **磁盘利用率高**：非叶子节点只存键值

**与 B 树区别**：
| 特性 | B 树 | B+ 树 |
|------|------|-------|
| 数据存储 | 所有节点 | 只有叶子节点 |
| 范围查询 | 需要中序遍历 | 叶子节点链表 |
| 树高度 | 较高 | 较低 |

**追问**：
- 聚簇索引和非聚簇索引区别？
- 为什么用自增主键？

### Q6: MVCC 原理？

**参考答案**：

**核心组件**：
1. **隐藏字段**：
   - DB_TRX_ID：事务 ID
   - DB_ROLL_PTR：回滚指针

2. **Undo Log**：版本链

3. **Read View**：可见性判断
   - m_ids：活跃事务 ID 列表
   - min_trx_id：最小活跃事务 ID
   - max_trx_id：下一个事务 ID

**可见性判断**：
- trx_id < min_trx_id → 可见
- trx_id >= max_trx_id → 不可见
- trx_id 在 m_ids 中 → 不可见

**隔离级别**：
- RC：每次 select 生成新的 Read View
- RR：事务第一次 select 生成 Read View

**追问**：
- MVCC 能解决幻读吗？
- 当前读和快照读区别？

### Q7: 主从复制？

**参考答案**：

**复制流程**：
1. Master 写入 Binlog
2. Slave IO 线程读取 Binlog，写入 Relay Log
3. Slave SQL 线程重放 Relay Log

**复制模式**：
- 异步复制：性能好，可能丢数据
- 半同步复制：至少一个从库确认
- 全同步复制：所有从库确认

**主从延迟解决**：
1. 并行复制
2. 读写分离优化（关键读走主库）
3. 分库分表

**追问**：
- 如何监控主从延迟？
- GTID 复制是什么？

### Q8: 分库分表？

**参考答案**：

**垂直分库**：按业务拆分
- 用户库、订单库、商品库

**垂直分表**：按字段拆分
- 大字段拆分到扩展表

**水平分库**：按数据拆分
- 用户 ID 取模分库

**水平分表**：按数据拆分
- 订单 ID 取模分表

**分片策略**：
- 范围分片：按时间、ID 范围
- Hash 分片：按 Hash 值取模
- 一致性 Hash：减少数据迁移

**问题**：
- 跨库 JOIN
- 分布式事务
- 全局唯一 ID
- 数据迁移

**追问**：
- 如何解决跨库 JOIN？
- 如何实现数据迁移？

---


## Redis 高频题


### Q9: Redis 为什么快？

**参考答案**：

1. **内存操作**：数据在内存中
2. **单线程**：无锁竞争
3. **IO 多路复用**：epoll
4. **高效数据结构**：SDS、跳表、压缩列表

**追问**：
- 为什么用单线程？
- Redis 6.0 为什么引入多线程？

### Q10: 缓存穿透、击穿、雪崩？

**参考答案**：

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 穿透 | 查询不存在的数据 | 布隆过滤器、缓存空值 |
| 击穿 | 热点 key 过期 | 永不过期、互斥锁 |
| 雪崩 | 大量 key 同时过期 | 随机过期时间、多级缓存 |

**追问**：
- 布隆过滤器原理？
- 如何设计多级缓存？

### Q11: Redis 集群？

**参考答案**：

| 方案 | 描述 | 特点 |
|------|------|------|
| 主从复制 | 一主多从 | 读性能提升 |
| 哨兵 | 监控+故障转移 | 高可用 |
| Cluster | 分片集群 | 读写扩展 |

**Cluster 原理**：
- 16384 个槽位
- CRC16(key) % 16384 定位槽位
- Gossip 通信

**追问**：
- Cluster 如何做故障转移？
- 如何处理热点 Key？

---


