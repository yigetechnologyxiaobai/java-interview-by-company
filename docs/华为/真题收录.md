# 华为 - 真题收录

> 本文档整理自 华为 面经真题，按知识点归类

## 目录

- [十、2026 暑期实习全流程面经](#十、2026-暑期实习全流程面经)
- [十一、2026 暑期实习面经（二）](#十一、2026-暑期实习面经（二）)

---

## 2026 暑期实习全流程面经


> 来源：牛客网 2026-05
> 特点：技术面 + 主管面，流程紧凑高效

### 技术一面

#### Q1: C 与 C++ 的主要区别？

| 特性 | C | C++ |
|------|---|-----|
| 编程范式 | 面向过程 | 多范式（面向对象+泛型） |
| 类 | 无 | 有 |
| 继承/多态 | 无 | 有 |
| 标准库 | 较少 | STL（容器、算法） |
| 引用 | 无 | 有 |
| 异常处理 | 无 | try-catch |

---

#### Q2: 什么是内存泄漏？如何检测与避免？

**常见原因**：
```c
// 1. 忘记释放
int* p = malloc(sizeof(int) * 100);
// 忘记 free(p);

// 2. 异常导致未执行 free
int* p = malloc(sizeof(int) * 100);
if (error) return;  // 提前返回，未释放
```

**检测工具**：Valgrind、AddressSanitizer、Dr. Memory

**避免方法**：
```cpp
// 使用智能指针（C++）
std::shared_ptr<int> p = std::make_shared<int>(10);

// RAII 模式
class Resource {
    int* data;
public:
    Resource() : data(new int[100]) {}
    ~Resource() { delete[] data; }  // 自动释放
};
```

---

#### Q3: new 和 delete 的底层实现？

**new 的过程**：
1. 调用 `operator new` 分配内存（底层是 malloc）
2. 调用构造函数初始化对象
3. 返回对象指针

**delete 的过程**：
1. 调用析构函数
2. 调用 `operator delete` 释放内存（底层是 free）

| 特性 | new | malloc |
|------|-----|--------|
| 返回类型 | 类型安全 | void* |
| 失败处理 | 抛异常 | 返回 NULL |
| 构造函数 | 自动调用 | 不调用 |

---

#### Q4: 堆与栈的区别？

| 特性 | 栈（Stack） | 堆（Heap） |
|------|------------|-----------|
| 分配方式 | 编译器自动 | 程序员手动 |
| 释放方式 | 自动释放 | 手动释放 |
| 大小限制 | 较小（MB 级） | 较大（GB 级） |
| 分配速度 | 快 | 慢 |
| 碎片问题 | 无 | 有 |

---

#### Q5: 什么是内存溢出？常见场景？

**堆溢出**：
```java
int[] arr = new int[Integer.MAX_VALUE];  // OOM
```

**栈溢出**：
```java
void recursion() {
    recursion();  // StackOverflowError
}
```

**排查方法**：
- `jmap -histo <pid>` 查看对象统计
- `jvisualvm` 分析堆内存

---

### 主管二面（软性问题）

1. **校期间的重要经历与荣誉**（STAR 法则讲述）
2. **为什么选择尽早进入职场？**（展现职业规划清晰性）
3. **项目中最大技术难题？**（具体问题 + 解决思路 + 收获）
4. **团队协作冲突如何协调？**（沟通方式 + 最终结果）
5. **如何看待加班文化？**（正面回应，强调效率而非时长）
6. **未来想在哪个城市发展？**（明确偏好 + 长期意愿）

---

**面试总结**：
- 技术面偏基础，重语言底层
- 主管面侧重稳定性、抗压能力
- 提前明确技术栈主次，避免跨语言深挖

---


## 2026 暑期实习面经（二）


> 来源：牛客网 2026-05
> 特点：技术面偏 C++ 基础，根据面试者回答调整技术栈

### 华为实习一面面经（2026-05-30）

**来源**：[华为26暑期实习面经](https://www.nowcoder.com/feed/main/detail/24b1719a101a4b13b51df76e61d27616)

#### Q1: C 与 C++ 的主要区别是什么？

**参考答案**：

| 特性 | C | C++ |
|------|---|-----|
| 编程范式 | 面向过程 | 多范式（面向对象+泛型） |
| 类与对象 | 无 | 有 |
| 继承/多态 | 无 | 有 |
| 标准库 | 较少 | STL（容器、算法、迭代器） |
| 引用 | 无 | 有 |
| 异常处理 | 无 | try-catch 机制 |
| 函数重载 | 不支持 | 支持 |
| 默认参数 | 不支持 | 支持 |

**代码示例**：
```cpp
// C 风格
struct Point { int x, y; };
void move(struct Point* p, int dx, int dy) {
    p->x += dx;
    p->y += dy;
}

// C++ 风格
class Point {
public:
    int x, y;
    void move(int dx, int dy) {
        x += dx;
        y += dy;
    }
};
```

**追问**：
- C++ 多态如何实现？
- 虚函数的作用是什么？

---

#### Q2: 什么是内存泄漏？如何检测和避免？

**参考答案**：

**定义**：程序动态分配的内存未被释放，导致内存无法被重新使用。

**常见原因**：
```c
// 1. 忘记释放
int* p = malloc(sizeof(int) * 100);
// 忘记 free(p);

// 2. 异常导致未执行 free
int* p = malloc(sizeof(int) * 100);
if (error) return;  // 提前返回，未释放

// 3. 指针被覆盖
int* p = malloc(sizeof(int) * 100);
p = malloc(sizeof(int) * 200);  // 原内存泄漏
```

**检测工具**：
- **Valgrind**：Linux 下最常用的内存检测工具
- **AddressSanitizer**：编译器内置，性能开销小
- **Dr. Memory**：Windows 平台

**避免方法**：
```cpp
// 使用智能指针（C++）
std::unique_ptr<int> p = std::make_unique<int>(10);
std::shared_ptr<int> sp = std::make_shared<int>(20);

// RAII 模式
class Resource {
    int* data;
public:
    Resource() : data(new int[100]) {}
    ~Resource() { delete[] data; }  // 自动释放
};
```

**追问**：
- 智能指针的实现原理？
- unique_ptr 和 shared_ptr 的区别？

---

#### Q3: new 和 delete 的底层实现？

**参考答案**：

**new 的过程**：
1. 调用 `operator new` 分配内存（底层通常是 malloc）
2. 调用构造函数初始化对象
3. 返回对象指针

**delete 的过程**：
1. 调用析构函数
2. 调用 `operator delete` 释放内存（底层通常是 free）

**代码示例**：
```cpp
// new 表达式
MyClass* obj = new MyClass();

// 等价于
void* memory = operator new(sizeof(MyClass));  // 分配内存
MyClass* obj = new(memory) MyClass();          // 定位 new，调用构造函数

// delete 表达式
delete obj;

// 等价于
obj->~MyClass();                    // 调用析构函数
operator delete(obj);               // 释放内存
```

**对比**：
| 特性 | new/delete | malloc/free |
|------|-----------|-------------|
| 返回类型 | 类型安全 | void* |
| 失败处理 | 抛出 bad_alloc | 返回 NULL |
| 构造/析构 | 自动调用 | 不调用 |
| 可重载 | 是 | 否 |

**追问**：
- placement new 是什么？
- 如何重载 operator new？

---

#### Q4: 堆与栈的区别？

**参考答案**：

| 特性 | 栈（Stack） | 堆（Heap） |
|------|------------|-----------|
| 分配方式 | 编译器自动管理 | 程序员手动申请/释放 |
| 释放方式 | 函数结束自动释放 | delete/free 手动释放 |
| 大小限制 | 较小（MB 级，约 1-8MB） | 较大（GB 级） |
| 分配速度 | 快（移动指针） | 慢（查找空闲块） |
| 碎片问题 | 无 | 有 |
| 生命周期 | 函数作用域 | 手动控制 |
| 存储内容 | 局部变量、函数参数 | 动态分配的对象 |

**代码示例**：
```cpp
void func() {
    int a = 10;          // 栈上
    static int b = 20;   // 静态区
    int* p = new int(30); // 堆上
    delete p;
}
```

**追问**：
- 为什么栈比堆快？
- 什么情况下使用堆？

---

#### Q5: 什么是内存溢出？常见场景？

**参考答案**：

**定义**：程序请求的内存超过了系统能提供的最大内存。

**常见场景**：

1. **堆溢出（OOM）**：
```java
// 创建超大数组
int[] arr = new int[Integer.MAX_VALUE];  // OutOfMemoryError

// 对象创建过快
List<Object> list = new ArrayList<>();
while (true) {
    list.add(new Object());  // 无限添加
}
```

2. **栈溢出（StackOverflowError）**：
```java
void recursion() {
    recursion();  // 无限递归
}
```

3. **内存泄漏导致溢出**：
```java
// 静态集合持有对象引用
static List<Object> cache = new ArrayList<>();
// 持续添加但不清理
```

**排查方法**：
```bash
# JVM 堆内存分析
jmap -histo <pid> | head -20

# 导出堆转储
jmap -dump:format=b,file=heap.hprof <pid>

# 使用 jvisualvm 或 MAT 分析
```

**追问**：
- 如何定位内存泄漏？
- JVM 内存模型？

---

### 华为实习主管二面面经（2026-05-30）

**来源**：[华为26暑期实习面经](https://www.nowcoder.com/feed/main/detail/24b1719a101a4b13b51df76e61d27616)

#### 软性问题汇总

1. **在校期间的重要经历与荣誉**（用 STAR 法则讲述）
2. **为什么选择尽早进入职场？**（展现职业规划清晰性）
3. **项目中遇到的最大技术难题？**（具体问题 + 解决思路 + 收获）
4. **团队协作中如何解决冲突？**（沟通方式 + 最终结果）
5. **多久给父母打一次电话？**（体现家庭责任感）
6. **如何看待华为加班文化？**（正面回应，强调效率而非时长）
7. **未来想在哪个城市发展？**（明确偏好 + 长期意愿）

**回答技巧**：
- 用 STAR 法则（Situation、Task、Action、Result）
- 展现稳定性和抗压能力
- 体现学习能力和团队协作精神

---

**面试总结**：
- 技术栈选择要明确，避免被追问不熟悉的语言
- 华为重视综合素质，软性问题也要认真准备
- 项目经验要能说清楚难点和解决方案

---

[返回目录](../README.md)

